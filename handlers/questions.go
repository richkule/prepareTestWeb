package handlers

import (
	in "github.com/richkule/prepareTestWeb/init"
	"net/http"
)

// Обрабатывает страницу редактирования вопроса еще в разработке
func editQuestion(w http.ResponseWriter, req *http.Request, sessUs *in.SessUs, quesId int) error {
	return nil
}
